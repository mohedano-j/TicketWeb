import * as React from "react";
import "./TableRow.scss";

export const TableRowEmpty = (props: any) => {
  return <div className={"table-row text-center"}>NO RECORDS FOUND</div>;
};

export default TableRowEmpty;
